To install SSDL for MSys, run make in this directory.

static-test tests the library; you can run it to verify that make worked (that is, that the library built and the program runs). 

Run it thus: ./runw
